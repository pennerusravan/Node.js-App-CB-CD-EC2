version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 18
  build:
    commands:
      - echo "Build started"
      - ls -la
      - zip -r artifact.zip .
artifacts:
  files:
    - artifact.zip
